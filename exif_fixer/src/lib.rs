/// Module declaration to read exif data from a given image
/// and fix orientation.
pub mod exif_data {
    pub fn exif_reader() {
        println!("Smile");
    }

    fn exif_fixer() {
        println!("Exif fixer");
    }
}

